        {% if offer.user is defined and offer.user is not null%}
             {% if offer.user.phone !='' %}
                 tel. {{offer.user.phone}}
                     {% if offer.user.mobile | length > 0 %}
                         <br/>kom.: {{offer.user.mobile}}
                     {% endif %}


             {% elseif offer.user.mobile %}
                  tel. {{offer.user.mobile}}
             {% endif %}
        {% elseif offer.office is defined and offer.office is not null%}

                     tel. {% if offer.office.user is defined %} {{offer.user.phone}} {% else %}{{offer.office.phone}}{% endif %}
                     {% if offer.user.mobile | length > 0 %}
                         <br/>kom.: {{offer.user.mobile | phoneMask}}
                     {% endif %}

        {% else %}
        {% if offer.phone !='' %}tel. {{offer.phone}}{% endif %}
        {% endif %}

