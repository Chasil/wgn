Nazwa;Typ Transakcji;Kategoria;Sygnatura;Osoba Kontaktowa;Promowane;Status;Data Dodania
{% for offer in offers %}
{{offer.name}};{{offer.transactionType}};{{offer.category}};{{offer.signature}};{{offer.contactPerson}};{% if offer.isPromo and date(offer.promoExpire) > date()%}TAK{% else %}NIE{% endif %};{% if offer.isPublish and date(offer.expireDate) > date()%}Aktywne{% else %}Nieaktywne{% endif %};{{offer.createDate | date('d.m.Y')}}{% endfor %}